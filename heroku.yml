build:
  docker:
    web:
      dockerfile: apps/web/Dockerfile
    scraper:
      dockerfile: apps/services/Dockerfile
    scheduler:
      dockerfile: apps/services/Dockerfile

run:
  web: npm start
  scraper: npm run serve
  scheduler: npm run cron