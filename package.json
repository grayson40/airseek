{
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:local": "turbo run dev --parallel",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^1.12.4"
  },
  "dependencies": {
    "string-similarity-js": "^2.1.4"
  }
}
